package com.lewa.filemanager;

import android.util.Log;

import com.lewa.themes.ThemeClientApplication;
import com.lewa.themes.ThemeManager;

public class FileManagerApplication extends ThemeClientApplication {
    @Override
    public void onCreate() {
        super.onCreate();
    }
}
